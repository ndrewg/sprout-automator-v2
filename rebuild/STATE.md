# STATE — where this build actually is

Read this first, before any phase file. The phase docs describe a *plan*; this describes the *reality*. When they disagree, this file wins and the phase file needs a correction note.

Last updated: **2026-08-13**. **Every phase is complete** — 0–3 from the original build, plus T, 4A.2, 4B, L, 5, 6, 7 and 8. Remaining work is **phases 9 → 10** (see *Suggested order* below), the rest of `BACKLOG.md`, and two `[manual]` items that need a real VPS. 4B was split across two rounds: mailer + password reset + idle timeout (round 1), then email verification + account deletion (round 2). **4B.6 (data export) and 4B.7 (monitoring hooks) were deliberately skipped** — export is ceremony for a handful of users, and admin visibility is ranked in `BACKLOG.md` instead — so 4B is functionally complete. Every `[manual]` check passed. **Tagged:** 0, 1, 2, 3, T, 4A2, 6, 7. **Untagged:** 4 (all of 4B), L, 5, 8 — the human tags, since only they can confirm the `[manual]` checks. **The phase-8 `TRUSTED_CLOUDFLARE_PEERS` hardening pass (F1–F5, `reviews/phase-8-cf-peer-hardening.md`) is committed in its own follow-up to `360d9b2`.** Its two footguns (the `normalizePeer` plain/mapped mismatch from tester B5, and the round-4 "trusted *too much*" set — `/0` prefixes, host-bits-set CIDRs, and newline-splitting boot errors from B8/B9/B10) are closed: a `/0` and a CIDR with host bits set now **refuse to boot** naming the corrected form, and the boot error escapes and caps the offending entry. The safety property is unchanged — with the key unset the gate is OFF and `CF-Connecting-IP` is never honoured. The one residual (B11) is message wording only: the `/0` refusal shows IPv4-form examples for both families. **The real Cloudflare Tunnel check in `reviews/phase-8-addendum.md` § D stays `[manual]` — it cannot run until a tunnel exists.**

---

## Built and verified

| Area | State | Evidence |
|---|---|---|
| Scaffold, config validation, health, pino logging | ✅ | tag `phase-0-complete` |
| Postgres 18 + Drizzle schema + migrations | ✅ | tag `phase-1-complete` |
| Argon2id auth, DB-backed signed-cookie sessions, audit log | ✅ | tag `phase-1-complete` |
| AES-256-GCM credentials, partial-update contract | ✅ | tag `phase-1-complete` |
| Playwright automation (login, OTP, clock, screenshots) | ✅ validated against **live HRHub** | tag `phase-2-complete` |
| Run queue, partial-unique-index race guard, orphan recovery | ✅ | tag `phase-2-complete` |
| IMAP OTP polling racing the manual paste bridge | ✅ | tag `phase-2-complete` |
| node-cron scheduler, Manila, Mon–Fri, PH holiday skip | ✅ | tag `phase-2-complete` |
| React 19 / Tailwind 4 / shadcn SPA, four panels, responsive | ✅ | tag `phase-3-complete` |
| SPA served from Express in the production image | ✅ | tag `phase-3-complete` |
| Helmet CSP + HSTS, rate limits, trust proxy, body cap | ✅ | commit `21a0971` — **4A only, not tagged** |
| Email verification + account deletion | ✅ | **4B.2/4B.5** — round 2. Verify tokens reuse `reset_tokens` with `purpose`, 24 h TTL, single-use. **A privilege escalation was found and fixed here unprompted:** `consumeResetToken` ignored `purpose`, so a 24 h verify token could be spent at the 1 h reset endpoint to change a password; purpose is now required and tested both directions. Deletion re-confirms the password, refuses while a run is active, unregisters cron, writes a surviving `account_deleted` audit row (`user_id` NULL + `emailHash`), cascades eight tables and removes the user's data directories. **Verification is deliberately NOT enforced** — with no mail provider the links only reach the server log, so gating actions on it would make the app unusable. All nine `[manual]` checks passed. **Not yet tagged** |
| Transactional email mailer + password reset + idle session timeout | ✅ | **4B.1/4B.3/4B.4** — environment-dependent dev/prod logging, single-use tokens, resettable rate-limit store for tests, 11th-is-429 property asserted at default. All nine `[manual]` checks passed. **Not yet tagged** |
| Test harness: `app.ts`/`index.ts` split, vitest unit+integration projects, 23 integration tests, Playwright e2e | ✅ | phase T — `[manual]` Docker check passed; not yet tagged |
| Run notifications + missed-run reconciliation | ✅ | commit `a81c96d`; six review rounds (defects 3, 12, 17, 18, 19, 20). `[manual]`: settings round-trip, test button, rate limit, enable guard, encrypted-at-rest, failure ⚠️, skipped ℹ️, missed 🔴 (incl. no-duplicate) all verified live — see `reviews/phase-6-addendum.md` § E. Success ✅ deferred (needs a real clock action). **Not yet tagged** |
| Run executor failure hardening (backlog #2 + #4) | ✅ | Executor rejection now marked `failure` with notification via `.catch` backstop; decrypt moved inside try/catch so corrupt `*_enc` triggers graceful failure not unhandled rejection; missed-run `notified_at` added for retry after Telegram outage; global unhandledRejection/uncaughtException handlers log as final backstop. New integration tests prove both paths. Setup scripts (`setup.ps1`/`setup.sh`) for first-run infrastructure + secret generation — **happy path unverified** (manual setup only; see scripts/setup.{ps1,sh} line 151-152). **Not yet tagged** |
| Signup gating — email allowlist (§ 4A.2) | ✅ | `SIGNUP_ALLOWED` takes domains and exact addresses; optional in dev (allow-all + warning), **required in production or the app refuses to boot**. All six `[manual]` checks passed live, including the substring attack (`notorchard.com.au` → 403) and grandfathering (an address removed from the list could no longer sign up but still logged in). Rejections audit an `emailHash` only. See `reviews/phase-4A2-addendum.md`. **Not yet tagged** |
| Linting wired up and proven (phase L) | ✅ | oxlint on **both** packages with the `correctness` category, plus `react/jsx-key` (`checkFragmentShorthand`), `react/exhaustive-deps`, and backend `no-floating-promises` / `no-misused-promises` / `prefer-await-to-then` (needs `oxlint-tsgolint` + `typeAware`). `pnpm lint` now runs in **CI and pre-commit**. Zero warnings baseline. **All four fault probes proven to fail the build**, including the fragment-shorthand missing key that triggered the round. The three sanctioned `.catch()` idioms carry narrow inline disables pointing at the rule. **Not tagged** |
| Pause / leave days (schedule pause window + controls) | ✅ | commit `e6c81f9`; gates 7A + 7B green (66 unit, 48 integration, e2e desktop + mobile). **All seven `[manual]` checks passed live** — banner, "Skip tomorrow", **a paused day suppressed a real cron fire**, **no missed-run alert while paused and the alert returned once unpaused**, manual runs still work while paused, clearing restores normal, and a past window auto-expired with no user action. Migration `0002_pause.sql` applied to the dev DB and `pnpm dev` verified booting. See `reviews/phase-7-addendum.md`. **Not yet tagged** |
| TLS deploy: `docker-compose.prod.yml`, `Caddyfile`, `DEPLOY.md`, `APP_URL` production guard, backups | ✅ | `docker-compose.prod.yml` + `Caddyfile` (both modes, localhost + domain); Caddy reverse proxy on 80/443, backend port reset; `DEPLOY.md` complete runbook; APP_URL production guard in `config.ts` with tests; `pg_dump --clean --if-exists` restore tested to scratch DB. VPS host hardening (§5.2) and live-domain TLS remain `[manual]`. Gate: `pnpm typecheck && pnpm test && pnpm test:integration && pnpm build` all green. Spec corrections: bare `:443 { tls internal }` doesn't work (no hostname for certs), use `localhost` instead; `docker exec -T` removed in Docker 29.5, use `-i` for stdin; Caddy ports parameterized (§5, overridable via env). See `reviews/phase-5-deploy-ops-addendum.md`. **Not yet tagged** |
| OTP acquisition retry path (production bugfix, 2026-08-11) | ✅ | The retry path is structurally fixed, gates green (110 unit + 99 integration). `src/services/otp-acquisition.ts` (`createOtpAcquirer`) races the manual bridge against IMAP per attempt with a **fresh `AbortController` each call** (was: one run-scoped controller, aborted after the first acquisition — killing the retry's IMAP poller). Every code handed out is remembered and passed as `excludeCodes` through `pollForOtp` → `fetchLatestOtp`, so a retry cannot re-acquire the same stale email. `errorSummary` (`lib/text.ts`) unwraps `AggregateError` into its joined causes where `runs.error` is persisted, so the Telegram alert carries the real diagnosis. Tests proven to fail against each reintroduced bug. `reference/crypto-and-otp-specs.md` carries the as-built note. **`[manual]` remaining: the only true proof is a live unattended run where the first OTP is stale and the retry must succeed via IMAP with no human present (the 16:15 clock-out on the operator's machine).** |
| Missed-run sweep clock consistency (test-suite defect, fixed with the OTP bugfix) | ✅ | `defaultSweepDeps.hasRunToday` filtered by the **wall clock** (`Date.now() - 24h`) while the sweep's `now` is injected; once the calendar passed 2026-08-10 the sweep tests' fixed-date runs fell outside the window and `test:integration` went red. `hasRunToday` now anchors its window to the sweep's injected `now`. No production behaviour change (both were `Date.now()` in prod), but the defect was real: the sweep's clock was injected everywhere except the run lookup. |
| Compose env passthrough + real-client-IP rate-limit keying (phase 8) | ✅ | gates 8A/8B/8C green (132 unit, 101 integration). `docker-compose.yml` passes every **optional** config dial (`APP_URL`, `AUTH_RATE_LIMIT`, `MAIL_FROM`, `MAX_CONCURRENT_RUNS`, `MISSED_RUN_GRACE_MINUTES`, `RESEND_API_KEY`, `SIGNUP_ALLOWED`, `TRUST_PROXY_HOPS`, `TRUSTED_CLOUDFLARE_PEERS`) with `${KEY:-}` interpolation — an empty default that silences the seven `variable is not set` warnings while keeping `config.ts` the sole source of real defaults; the required secrets `APP_ENCRYPTION_KEY`/`SESSION_SECRET` stay **bare** `${KEY}` so a missing value still warns and the boot guard fires. `config.ts` treats the empty string Compose emits for an unset key exactly like an unset key, so a dial behaves identically inside and outside Docker.  config.ts treats the empty string Compose emits for an unset key exactly like an unset key, so a dial behaves identically inside and outside Docker. The tracked `jar` cookie file is removed and `.gitignore` widened (`jar`, `cookie*`, `*.cookies`). `AUTH_RATE_LIMIT` default 10→30; the budget-enforced integration test derives its loop from `config.AUTH_RATE_LIMIT` so the next default change can't break it. `authLimiter`/`apiLimiter` key on `clientIp(req)` — `CF-Connecting-IP` honoured ONLY when the request's socket peer is a trusted Cloudflare tunnel peer (**none exists today, so the header is ignored and keying falls back to `req.ip`**, driven by `TRUST_PROXY_HOPS`, default 1); a spoofed header cannot merge every client into one bucket, nor rotate the header for a fresh bucket (evasion), nor claim a victim's address to fill their bucket (poisoning). The first review round shipped the weaker "parse as an IP literal" gate, which the tester proved bypassable (evasion + poisoning probes against the real app) — the trusted-peer gate is the correction. `docker-compose.prod.yml` keeps only its genuine overrides (APP_URL placeholder, empty-passthroughs, `NODE_ENV=production`, `ports: !reset []`) — the numeric dials carry no default there either. The `[manual]` Docker boot checks from the phase-8 file: the three rate-limit assertions are now **verified** by the round-3 tester (live against the container); the Cloudflare Tunnel assertion (row 6 of the addendum) remains outstanding. **Not yet tagged** — reviewer independently re-ran all gates (lint/typecheck clean, 132 unit, 101 integration) and closed review round 2; review round 3 re-ran every gate green and the rate-limit `[manual]` rows are now **verified** (16th request 429 at `AUTH_RATE_LIMIT=15`; 31st at the default 30; clean boot, no warnings). The sole outstanding `[manual]` is a real Cloudflare Tunnel in front (row 6 of the addendum) — only a human can run it. The trusted-peer gate (B1) was verified in round 2 by reading `middleware/security.ts`, which honours `CF-Connecting-IP` only when `req.socket.remoteAddress` is in `trustedCloudflarePeers` (empty default, so the header is ignored in every deployment today and keying falls back to `req.ip`). Residuals B4/B5 below. **Round 3 (2026-08-12) closed the reviewer's follow-ups:** the compose passthrough switched from bare `${KEY}` to `${KEY:-}` for every optional key (APP_ENCRYPTION_KEY/SESSION_SECRET stay bare — a missing value there SHOULD warn), killing the seven per-command "variable is not set" warnings while keeping the identical empty-string passthrough; the empty-string→default contract for `AUTH_RATE_LIMIT`/`MAX_CONCURRENT_RUNS`/`TRUST_PROXY_HOPS` is now pinned by unit tests (`config-defaults.test.ts`). The trusted-peer set is no longer a code constant you must edit: it is populated from a new **`TRUSTED_CLOUDFLARE_PEERS`** env key (comma-separated peer addresses, empty = gate off) wired through `config.ts`, `.env.example` and both compose files, so the §8C tunnel path is operator-opt-in rather than unreachable — a `parseTrustedCloudflarePeers` unit test pins the parse (empty string = empty set), and the DEPLOY.md §4.1 note tells the operator what to set and the consequence of forgetting. `COREPACK_ENABLE_DOWNLOAD_PROMPT=0` added to both Dockerfile stages and both setup scripts. Live re-verification: `AUTH_RATE_LIMIT=15` → 16th login 429; unset → 31st login 429; `docker compose config` renders 0 "is not set" warnings; rebuild log has no corepack prompt; `git ls-files jar` empty; drizzle untouched. |

The app runs today via `docker compose up -d --build` on `http://localhost:3000` (see `RUNNING.md`). Backend tests live in `app/backend/test/` mirroring `src/`.

## Not built

| Area | Where it's specified | Blocking? |
|---|---|---|
| Everything else | `BACKLOG.md` | ranked there |

## Suggested order from here

All phases are done. What is left:

1. **The OTP retry defect — fixed (2026-08-11), reviewed and committed.** All gates green (`pnpm lint && pnpm typecheck && pnpm test` and `pnpm test:integration`), re-run independently by the reviewer. The only remaining step is the `[manual]` live check: a real unattended scheduled run where the first OTP is stale and the retry succeeds via IMAP with no human present (the 16:15 clock-out on the operator's machine). See the "Built and verified" row and the corrected "Known gaps" entry below.
2. **[`phases/phase-9-runs-history.md`](./phases/phase-9-runs-history.md)** — **the next build target.** `GET /runs` gains a `limit` param and `hasMore` (runs 21+ are currently *unreachable*, not paginated), the panel gains dates + `Today`/`Yesterday` + "Show more", and `CredentialsPanel` finally states the Gmail-only constraint. **Depends on phase 8, which has shipped** — 8A is the prerequisite for the environment as a whole.
3. **[`phases/phase-10-admin-visibility.md`](./phases/phase-10-admin-visibility.md)** — `ADMIN_EMAILS` + boot reconciliation + `requireAdmin` + `GET /admin/overview` + panel. **Specced but gated: do not build until a second person has an account** — with one user the overview is one row.

   > **Re-scoped 2026-08-12 by brainstorm.** A single six-gate phase 8 was decomposed into these three after settling on a ~5-person pilot rather than a 30-user launch. Screenshot pruning, the email-keyed limiter rewrite and admin-before-UI were each considered and deliberately deferred — reasoning recorded in `BACKLOG.md` §§ 2, 3, 8. **Phase 8 (compose passthrough + `AUTH_RATE_LIMIT` 10→30 + real-client-IP keying) shipped 2026-08-12**; its three `[manual]` Docker boot checks are outstanding.
4. **`BACKLOG.md`** — twelve ranked items (§ 1 done; §§ 4, 5 promoted into phases 8/9; § 4 closed). **Re-ranked 2026-08-11 around a possible 30-user rollout**, which turns three of them from nice-to-have into blockers: #3 (IP-keyed rate limiter — 30 people behind one corporate NAT exhaust a 10/15min budget in seconds), #4 (compose drops seven config keys, and #3's cheapest remedy is unreachable without it), and #5 (onboarding + the Gmail-only constraint — the number of colleagues on Microsoft 365 is still unknown and determines the real user count). #2 (screenshot pruning) follows close behind at that scale.
5. **The two `[manual]` deploy items** — VPS host hardening (`phase-5-deploy-ops.md` § 5.2) and live-domain TLS. Both need a real host; everything else about the deploy is proven locally.

Working prompts for each round are in [`SESSION-PROMPT.md`](./SESSION-PROMPT.md).

## Known gaps in what *is* built

- **✅ The tracked curl cookie jar is REMOVED (phase 8 §8A).** `jar` — a Netscape cookie file holding a live `sid` session cookie for `127.0.0.1`, committed in `a81c96d` (phase 6) during manual API testing — was `git rm`'d, and `.gitignore` was widened from `cookies.txt` to `jar`, `cookie*`, `*.cookies`. **It is still in git history**, so removing the file does not remove the value; low severity (localhost-scoped, DB-backed sessions), and history rewriting was deliberately skipped per the phase file. The lesson stands: a guard that matches one filename is not a guard.
- **`is_admin` is inert.** The column exists (`db/schema.ts:24`), `publicUser` returns it (`routes/auth.ts:75`) and the frontend types it (`api.ts:19`) — and **nothing sets it and nothing reads it.** No `requireAdmin`, no admin route, no grant path, so no one can become an admin, including the operator. Specced in `phases/phase-10-admin-visibility.md` § 10A, which has to build the granting mechanism before it can build anything admin-only. **Gated on a second user existing.**

- **✅ The OTP retry path defect (found 2026-08-11, live scheduled run) is FIXED.** The original failure chain, for the record: `services/runs.ts` created **one** `AbortController` per run, aborted after the first OTP acquisition, so the retry's `pollForOtp` threw `IMAP polling aborted` without contacting Gmail — IMAP was dead for the rest of the run and the manual bridge timed out at 05:30. Compounded by `pollForOtp`'s 300 s `lookbackSeconds` default letting the first attempt accept a **stale** code already in the inbox, causing the retry. And `AggregateError.message` is always the literal `"All promises were rejected"`, which `executeQueuedRun` persisted verbatim — zero diagnostic value. **Fix:** `createOtpAcquirer` (fresh `AbortController` per attempt + per-run `submittedCodes` exclusion plumbed through `pollForOtp` → `fetchLatestOtp`) + `errorSummary` unwrapping `AggregateError` into its joined causes. Regression, unit and integration tests cover the retry path now (each proven able to fail). **Residual `[manual]` proof: a live unattended run with a stale first OTP whose retry succeeds via IMAP — the 16:15 clock-out on the operator's machine.**

- **Phases 0–5 gates are prose, not commands.** They were verified by hand at the time and are not re-runnable. `reference/testing-strategy.md` § "The high-value tests" lists what to backfill; the race guard and tenant isolation now have tests (phase T).
- **Frontend logout once dropped the user's session without unmounting the Dashboard** — `qc.clear()` empties the cache but does not notify the `["me"]` observer in AuthGate, so it kept its stale user. Fixed in phase T by switching `useLogout` to `qc.resetQueries()` (notifies subscribers + refetches); caught by the e2e smoke flow.
- **The e2e webServer builds the SPA into `app/backend/public` and migrates `sprout_test`** before booting the backend — so `pnpm test:e2e` needs Postgres running (`docker compose up -d postgres`) and writes into the same `sprout_test` database the integration suite uses.
- **E2E depends on a Chromium the frontend never installed.** `@playwright/test` is a frontend devDependency (pinned `1.60.0`, matching the backend's `playwright` pin — bump both together), but it is **not** in the frontend's `pnpm-workspace.yaml` `allowBuilds`, so pnpm blocks its browser-download postinstall. It works today only because the backend's `playwright install chromium` populated the shared `~/.cache/ms-playwright`. On a fresh machine that installs the frontend without the backend — or in CI, if e2e is ever added there — `pnpm test:e2e` fails with "browser not installed". Fix when it bites: add `'@playwright/test': true` to the frontend `allowBuilds`, or run `pnpm exec playwright install chromium` as a documented setup step.
- **IDE type errors may be spurious.** `tsc -b` on the frontend is clean; editors showing `Property 'error' does not exist` on the discriminated unions in `CredentialsPanel`/`NotificationsPanel` are using their bundled TypeScript rather than the workspace's TS 6. Select the workspace version, or restart the TS server.
- **`_archive/` holds a live `.env` and session cookies.** Gitignored, reference-only, forbidden by the guardrails. Should be deleted — `BACKLOG.md` § 8.

- **Residual (pre-existing, NOT a phase-8 regression): `X-Forwarded-For` can still rotate the auth bucket in the base direct-exposure deployment.** `TRUST_PROXY_HOPS` defaults to 1 and the base compose publishes the backend directly on `localhost:3000`, so a client that connects straight to the backend and rotates `X-Forwarded-For` gets a fresh `req.ip` (and thus a fresh rate-limit bucket) per request — the same evasion B1 described, via a different header. Safe behind Caddy (it appends the real client IP, so `req.ip` stays unforgeable). Recommend loopback-binding the published port (`"127.0.0.1:${BACKEND_PORT:-3000}:3000"`) and/or `TRUST_PROXY_HOPS=0` when the backend is not behind a proxy. Found in the phase-8 round-2 review (tester B4); deliberately not blocking the commit because it predates phase 8 and is unreachable behind the intended Caddy deployment.
- **✅ The `normalizePeer` / `TRUSTED_CLOUDFLARE_PEERS` footguns (tester B5, round 2) are FIXED — hardening pass 2026-08-13.** Every parsed entry now runs through the same `normalizePeer` as the socket peer (so `::ffff:172.20.0.4` in config matches a `172.20.0.4` peer and vice versa, and case differences can't silently miss); a malformed entry refuses to boot in `loadConfig()` naming its position; entries may be CIDR ranges (`172.20.0.0/16` — cloudflared's container IP decays when Docker renumbers); startup logs the set **count**; and a `CF-Connecting-IP` from a peer outside a non-empty set logs a once-only `warn` with the observed peer (never with the empty set). Details in the phase-8 § 8C as-built note.
- **✅ The three "trusted too much" values (tester round-4 B8–B10) are FIXED — 2026-08-13.** `0.0.0.0/0` and `::/0` (mask 0 → match EVERY peer, re-opening the spoofing hole while reporting as armed) and a CIDR with host bits set (`172.20.0.5/16` → silently the whole `172.20.0.0/16`) both now refuse to boot naming the corrected form — the network, or the single address with its full mask `/32`/`/128`. The boot refusal no longer interpolates the raw entry (a newline could split the log line); echoed entries are escaped and capped. `DEPLOY.md` § 4.1 shows the two correct forms. **B11 (minor, round-5):** the `/0` refusal message shows IPv4-form examples for both families (e.g. `"172.20.0.0/16" or "172.20.0.5/32"`), so an IPv6-only operator must translate to `2001:db8::/32` / `2001:db8::1/128`. Message wording only; non-blocking.
- **Partial: the `TRUSTED_CLOUDFLARE_PEERS` wiring (tester B7) is now single-line and observable.** `resetRateLimits()` no longer re-reads config — F5 made it restore the **empty** set, so the module-load line in `security.ts` is the ONLY config-derived population; F3's startup log (`trustedCloudflarePeerCount()`) surfaces a wiring break in the boot log. What remains unwritten is the belt-and-braces test that sets the env var before importing `security` and asserts the module picks it up — tampering with the module-load line alone still leaves the suite green. Non-blocking; flag for a later pass.
- **✅ `.cortexkit/` (Magic Context / opencode tool state) is now gitignored.** It was untracked and not ignored during phase 8; `.gitignore` gained `.cortexkit/` so a blanket `git add` can never stage its nested `.gitignore`. No sensitive content (empty `magic-context/historian/`).

## Conventions that changed after the phase docs were written

- **Audience.** Phases 0–5 were written for a quantized local model driven one phase per session. The build now targets a hosted agentic model (see `00-START-HERE.md` § "Model & runtime"). Phases 0–5 are kept **as an as-built record** — don't rewrite them; they document what was done and why.
- **Gates.** New phases state gates as commands with exit codes, marking human-only checks `[manual]`. See `reference/testing-strategy.md`.
- **Tests** live in `test/` mirroring `src/`, not colocated (commit `8c3d226`).
- **Test layout (phase T):** `test/lib/**`, `test/services/**` are the database-free `unit` vitest project (`pnpm test`); `test/integration/**` is the `integration` project against `sprout_test` (`pnpm test:integration`, single fork so the shared DB is never truncated concurrently). E2E lives in `app/frontend/e2e/` (`pnpm test:e2e`), driven by a `webServer` that builds the SPA into `app/backend/public`, migrates `sprout_test`, and boots the real backend.
